#include <bits/stdc++.h>

using namespace std;

int main()
{
    int a, b;
    cin >> a >> b;

    int steps = 0;

    while(a!=b)
    {
        if(a == 0 || b == 0)
        {
            steps=-1;
            break;
        }
        else
    }
}